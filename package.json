{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna publish",
    "lint": "eslint --fix --ext .js ./"
  },
  "devDependencies": {
    "alfred-flexible": "^1.0.0",
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
